server:
  port: 8761
  shutdown: graceful
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
spring:
    application:
      name: service-discovery
    main:
      cloud-platform: kubernetes
management:
  endpoints:
    web:
      exposure:
        include: "*"